import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { InvalidPage } from './invalid';

@NgModule({
  declarations: [
    InvalidPage,
  ],
  imports: [
    IonicPageModule.forChild(InvalidPage),
  ],
})
export class InvalidPageModule {}
