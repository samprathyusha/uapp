import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { ValidPage } from './valid';

@NgModule({
  declarations: [
    ValidPage,
  ],
  imports: [
    IonicPageModule.forChild(ValidPage),
  ],
})
export class ValidPageModule {}
